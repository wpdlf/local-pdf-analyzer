node_modules/
out/
dist/
# R37 P4-2 (v0.18.23): vitest --coverage 산출물 (v8 provider 의 lcov/text 리포트, ~5MB).
# CI 통합 시 별도 artifact 로 업로드 예정이므로 working tree 추적 금지.
coverage/
*.exe
*.dmg
*.zip
.env
.env.local
.bkit/
.claude/
# R29 (v0.18.13): CLAUDE.md 는 프로젝트에 체크인된 문서이므로 ignore 하지 않는다.
# (이전엔 tracked 상태와 ignore 가 충돌해 contributor 가 변경을 git status 로 못 보던 모순)

# Playwright E2E artifacts
test-results/
playwright-report/
